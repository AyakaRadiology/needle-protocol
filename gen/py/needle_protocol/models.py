# AUTO-GENERATED BY tools/gen.sh. DO NOT EDIT.
#
# Source: every schema under schemas/ (datamodel-code-generator)
# Regenerate with `tools/gen.sh`; CI fails on any drift.

# generated by datamodel-codegen:
#   filename:  needle-protocol.schema.json

from __future__ import annotations

from enum import Enum
from typing import Any, Literal

from pydantic import AwareDatetime, BaseModel, ConfigDict, Field, conint, constr


class AngleStreamEnvelopeBaseType(Enum):
    inclination = 'inclination'
    theta = 'theta'
    heartbeat = 'heartbeat'


class AngleStreamEnvelopeBase(BaseModel):
    model_config = ConfigDict(
        extra='allow',
    )
    v: Literal[1] = Field(
        ...,
        description='Envelope version. Deliberately NOT bumped by adding a frame type: `type` is what selects the payload.',
    )
    kind: Literal['evt']
    type: AngleStreamEnvelopeBaseType = Field(
        ...,
        description='Selects the payload. `inclination` is canonical; `theta` is the deprecated dual-emit copy; `heartbeat` carries liveness only.',
        title='AngleStreamEnvelopeBaseType',
    )
    ts: AwareDatetime = Field(
        ...,
        description='Host wall-clock ISO-8601 stamp taken when the frame was built. The two angle frames describing one sample carry the SAME ts.',
    )
    payload: dict[str, Any]


class AngleStreamFrameHeartbeatLink(BaseModel):
    model_config = ConfigDict(
        extra='allow',
    )
    state: str = Field(
        ...,
        description="needle-guide's SerialPortClient link state, verbatim. Free-form on the guide side: carry it, show it, never branch on it.",
    )
    live: bool = Field(
        ...,
        description='True only for `connected`, i.e. sensor samples are actually flowing.',
    )


class AngleStreamFrameHeartbeat(BaseModel):
    model_config = ConfigDict(
        extra='allow',
    )
    calibrated: bool = Field(
        ...,
        description='True once needle-guide has captured a zero-reference vector. False means any angle it reports is measured from an unknown reference.',
    )
    link: AngleStreamFrameHeartbeatLink = Field(
        ..., title='AngleStreamFrameHeartbeatLink'
    )
    protocol_version: conint(ge=1) | None = Field(
        None,
        description="OPTIONAL. The angle-stream wire version the emitter speaks. Rides the heartbeat so liveness and version arrive on the same tick: a subscriber that has a heartbeat has, by construction, everything it needs to decide whether it can read the angle frames. Absent from an emitter built before this field existed, which a subscriber must read as 'unknown', never as 1.",
    )
    protocol_package_version: (
        constr(
            pattern=r'^(0|[1-9]\d*)\.(0|[1-9]\d*)\.(0|[1-9]\d*)(?:-((?:0|[1-9]\d*|\d*[a-zA-Z-][0-9a-zA-Z-]*)(?:\.(?:0|[1-9]\d*|\d*[a-zA-Z-][0-9a-zA-Z-]*))*))?(?:\+([0-9a-zA-Z-]+(?:\.[0-9a-zA-Z-]+)*))?$'
        )
        | None
    ) = Field(
        None,
        description='OPTIONAL. Semver of the needle-protocol package the emitter was built against. Diagnostic, not a compatibility gate -- `protocol_version` is the gate. It exists so an operator staring at two disagreeing screens can tell which side is behind without opening either build.',
    )


class AngleStreamFrameInclinationLink(BaseModel):
    model_config = ConfigDict(
        extra='allow',
    )
    state: str = Field(
        ...,
        description="needle-guide's SerialPortClient link state, verbatim. Free-form on the guide side: carry it, show it, never branch on it.",
    )
    live: bool = Field(
        ...,
        description='True only for `connected`, i.e. sensor samples are actually flowing.',
    )


class AngleStreamFrameInclination(BaseModel):
    model_config = ConfigDict(
        extra='allow',
    )
    inclination_deg: float | None = Field(
        ...,
        description="Degrees from vertical. Explicit null is a sample the sensor could not resolve -- legal, and it means 'no angle'. It is NOT zero, and a subscriber that renders it as zero prints a vertical needle that is not there.",
    )
    calibrated: bool
    link: AngleStreamFrameInclinationLink = Field(
        ..., title='AngleStreamFrameInclinationLink'
    )


class AngleStreamFrameThetaLink(BaseModel):
    model_config = ConfigDict(
        extra='allow',
    )
    state: str = Field(
        ...,
        description="needle-guide's SerialPortClient link state, verbatim. Free-form on the guide side: carry it, show it, never branch on it.",
    )
    live: bool = Field(
        ...,
        description='True only for `connected`, i.e. sensor samples are actually flowing.',
    )


class AngleStreamFrameTheta(BaseModel):
    model_config = ConfigDict(
        extra='allow',
    )
    theta: float | None = Field(
        ...,
        description="CONSOLE_THETA_VERTICAL_DEG - inclination_deg. 90 = vertical, decreasing as the needle lies over. Explicit null means 'no angle', not zero.",
    )
    calibrated: bool
    link: AngleStreamFrameThetaLink = Field(..., title='AngleStreamFrameThetaLink')


class DeviceEnvelopeBaseType(Enum):
    status = 'status'
    data = 'data'
    error = 'error'
    log = 'log'


class DeviceEnvelopeBase(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    v: Literal[1]
    kind: Literal['evt']
    type: DeviceEnvelopeBaseType = Field(..., title='DeviceEnvelopeBaseType')
    seq: conint(ge=0) = Field(
        ...,
        description='Monotonic sequence number per boot/session (uint32_t on device).',
    )
    t_boot_ms: conint(ge=0) = Field(
        ...,
        description='ms since device boot, monotonic (uint64_t on device). NOT wall-clock; the host correlates to absolute time on receipt. Named for its clock so no host mistakes it for a timestamp.',
    )
    payload: Any


class DevicePayloadData(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    accel_x: float
    accel_y: float
    accel_z: float


class DevicePayloadError(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    code: constr(min_length=1)
    message: str
    detail: str | None = None


class DevicePayloadLogLevel(Enum):
    trace = 'trace'
    debug = 'debug'
    info = 'info'
    warn = 'warn'
    error = 'error'
    critical = 'critical'


class DevicePayloadLog(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    level: DevicePayloadLogLevel = Field(..., title='DevicePayloadLogLevel')
    message: str


class DevicePayloadStatusState(Enum):
    init = 'init'
    ready = 'ready'
    running = 'running'
    degraded = 'degraded'
    lost = 'lost'
    stopping = 'stopping'
    stopped = 'stopped'


class DevicePayloadStatus(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    state: DevicePayloadStatusState = Field(..., title='DevicePayloadStatusState')


class TrackerHello(BaseModel):
    model_config = ConfigDict(
        extra='allow',
    )
    type: Literal['hello']
    protocol_version: conint(ge=1) = Field(
        ...,
        description='Bumped only when the wire schema changes in a way the client must opt into (field rename, type change, new REQUIRED field). See TRACKER_SUPPORTED_PROTOCOL_VERSIONS.',
    )
    server_bind: str | None = Field(
        None,
        description='The address the tracker bound. Used by the desktop path indicator to confirm which interface is carrying the stream.',
    )
    client_addr: str | None = Field(
        None,
        description="The peer address the tracker sees for this client, or 'unknown'.",
    )

# ---------------------------------------------------------------------------
# Tagged unions, appended by tools/gen-py.py from the schemas' if/then tables.
# ---------------------------------------------------------------------------
from typing import Annotated  # noqa: E402,F811

from pydantic import Field  # noqa: E402,F811

class AngleStreamEnvelopeInclination(AngleStreamEnvelopeBase):
    """`schemas/angle-stream/envelope.json` with `type == "inclination"`."""

    type: Literal['inclination']
    payload: AngleStreamFrameInclination


class AngleStreamEnvelopeTheta(AngleStreamEnvelopeBase):
    """`schemas/angle-stream/envelope.json` with `type == "theta"`."""

    type: Literal['theta']
    payload: AngleStreamFrameTheta


class AngleStreamEnvelopeHeartbeat(AngleStreamEnvelopeBase):
    """`schemas/angle-stream/envelope.json` with `type == "heartbeat"`."""

    type: Literal['heartbeat']
    payload: AngleStreamFrameHeartbeat


class DeviceEnvelopeStatus(DeviceEnvelopeBase):
    """`schemas/device/envelope.json` with `type == "status"`."""

    type: Literal['status']
    payload: DevicePayloadStatus


class DeviceEnvelopeData(DeviceEnvelopeBase):
    """`schemas/device/envelope.json` with `type == "data"`."""

    type: Literal['data']
    payload: DevicePayloadData


class DeviceEnvelopeError(DeviceEnvelopeBase):
    """`schemas/device/envelope.json` with `type == "error"`."""

    type: Literal['error']
    payload: DevicePayloadError


class DeviceEnvelopeLog(DeviceEnvelopeBase):
    """`schemas/device/envelope.json` with `type == "log"`."""

    type: Literal['log']
    payload: DevicePayloadLog


#: `schemas/angle-stream/envelope.json`, tagged on `type` so that narrowing the frame
#: narrows its payload. The schema's `allOf` if/then table renders as
#: `payload: dict[str, Any]` on its own -- a model that would accept any
#: payload under any `type` and report success.
AngleStreamEnvelope = Annotated[
    AngleStreamEnvelopeInclination | AngleStreamEnvelopeTheta | AngleStreamEnvelopeHeartbeat,
    Field(discriminator='type'),
]


#: `schemas/device/envelope.json`, tagged on `type` so that narrowing the frame
#: narrows its payload. The schema's `allOf` if/then table renders as
#: `payload: dict[str, Any]` on its own -- a model that would accept any
#: payload under any `type` and report success.
DeviceEnvelope = Annotated[
    DeviceEnvelopeStatus | DeviceEnvelopeData | DeviceEnvelopeError | DeviceEnvelopeLog,
    Field(discriminator='type'),
]
