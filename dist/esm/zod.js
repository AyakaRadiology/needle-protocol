/**
 * AUTO-GENERATED BY tools/gen.sh. DO NOT EDIT.
 *
 * Source: every schema under schemas/ (json-schema-to-zod)
 * Regenerate with `tools/gen.sh`; CI fails on any drift.
 */
/**
 * Runtime validators. OPTIONAL: `zod` is a peer dependency and nothing in
 * `needle-protocol`'s default entry point imports this module.
 *
 * The 60 Hz angle-frame parse path in needle-simulator is hand-written on
 * purpose and must stay that way — a schema library in that loop is a
 * dependency in the hot path. Use these where the cost is free: tests,
 * fixtures, capture validation, and the first frame of a new connection.
 */
import { z } from 'zod';
/** `schemas/angle-stream/frame-heartbeat.json` */
export const AngleStreamFrameHeartbeatSchema = z.object({ "calibrated": z.boolean(), "link": z.object({ "state": z.string(), "live": z.boolean() }).catchall(z.any()), "protocol_version": z.number().int().gte(1).optional(), "protocol_package_version": z.string().regex(new RegExp("^(0|[1-9]\\d*)\\.(0|[1-9]\\d*)\\.(0|[1-9]\\d*)(?:-((?:0|[1-9]\\d*|\\d*[a-zA-Z-][0-9a-zA-Z-]*)(?:\\.(?:0|[1-9]\\d*|\\d*[a-zA-Z-][0-9a-zA-Z-]*))*))?(?:\\+([0-9a-zA-Z-]+(?:\\.[0-9a-zA-Z-]+)*))?$")).optional() }).catchall(z.any());
/** `schemas/angle-stream/frame-inclination.json` */
export const AngleStreamFrameInclinationSchema = z.object({ "inclination_deg": z.union([z.number(), z.null()]), "calibrated": z.boolean(), "link": z.object({ "state": z.string(), "live": z.boolean() }).catchall(z.any()) }).catchall(z.any());
/** `schemas/angle-stream/frame-theta.json` */
export const AngleStreamFrameThetaSchema = z.object({ "theta": z.union([z.number(), z.null()]), "calibrated": z.boolean(), "link": z.object({ "state": z.string(), "live": z.boolean() }).catchall(z.any()) }).catchall(z.any());
/** `schemas/device/payload-data.json` */
export const DevicePayloadDataSchema = z.object({ "accel_x": z.number(), "accel_y": z.number(), "accel_z": z.number() }).strict();
/** `schemas/device/payload-error.json` */
export const DevicePayloadErrorSchema = z.object({ "code": z.string().min(1), "message": z.string(), "detail": z.string().optional() }).strict();
/** `schemas/device/payload-log.json` */
export const DevicePayloadLogSchema = z.object({ "level": z.enum(["trace", "debug", "info", "warn", "error", "critical"]), "message": z.string() }).strict();
/** `schemas/device/payload-status.json` */
export const DevicePayloadStatusSchema = z.object({ "state": z.enum(["init", "ready", "running", "degraded", "lost", "stopping", "stopped"]), "envelope_schema_version": z.string().regex(new RegExp("^\\d+\\.\\d+\\.\\d+$")).optional() }).strict();
/** `schemas/plan-channel/payload-plan.json` */
export const PlanChannelPayloadPlanSchema = z.object({ "plan_inclination_deg": z.number().gte(0).lte(90), "azimuth_deg": z.number().gte(-180).lte(180).optional(), "entry_lateral_mm": z.number().gte(-1000).lte(1000).optional(), "entry_longitudinal_mm": z.number().gte(-3000).lte(3000).optional(), "plan_id": z.string().regex(new RegExp("^[A-Za-z0-9._-]{1,128}$")), "plan_revision": z.number().int().gte(0), "source": z.object({ "app": z.literal("needle-simulator"), "app_version": z.string().min(1), "protocol_package_version": z.string().regex(new RegExp("^(0|[1-9]\\d*)\\.(0|[1-9]\\d*)\\.(0|[1-9]\\d*)(?:-((?:0|[1-9]\\d*|\\d*[a-zA-Z-][0-9a-zA-Z-]*)(?:\\.(?:0|[1-9]\\d*|\\d*[a-zA-Z-][0-9a-zA-Z-]*))*))?(?:\\+([0-9a-zA-Z-]+(?:\\.[0-9a-zA-Z-]+)*))?$")) }).strict() }).strict();
/** `schemas/plan-channel/payload-plan-ack.json` */
export const PlanChannelPayloadPlanAckSchema = z.object({ "plan_id": z.string().regex(new RegExp("^[A-Za-z0-9._-]{1,128}$")), "plan_revision": z.number().int().gte(0), "result": z.enum(["applied", "pending_confirm", "rejected"]), "reason": z.string().min(1).optional(), "applied_revision": z.number().int().gte(0).optional() }).strict();
/** `schemas/tracker/hello.json` */
export const TrackerHelloSchema = z.object({ "type": z.literal("hello"), "protocol_version": z.number().int().gte(1), "server_bind": z.string().optional(), "client_addr": z.string().optional() }).catchall(z.any());
/** Shared fields of `schemas/angle-stream/envelope.json`, before the payload is pinned down. */
const AngleStreamEnvelopeBase = z.object({ "v": z.literal(1), "kind": z.literal("evt"), "type": z.enum(["inclination", "theta", "heartbeat"]), "ts": z.string().datetime({ offset: true }), "payload": z.record(z.string(), z.any()) }).catchall(z.any());
/**
 * `schemas/angle-stream/envelope.json`
 *
 * A discriminated union rather than the schema's literal `allOf`: json-schema-to-zod
 * renders an if/then table as `z.intersection(z.any(), z.any())`, which accepts any
 * payload on any `type` and reports success.
 */
export const AngleStreamEnvelopeSchema = z.discriminatedUnion('type', [
    AngleStreamEnvelopeBase.extend({ type: z.literal('inclination'), payload: AngleStreamFrameInclinationSchema }),
    AngleStreamEnvelopeBase.extend({ type: z.literal('theta'), payload: AngleStreamFrameThetaSchema }),
    AngleStreamEnvelopeBase.extend({ type: z.literal('heartbeat'), payload: AngleStreamFrameHeartbeatSchema }),
]);
/** Shared fields of `schemas/device/envelope.json`, before the payload is pinned down. */
const DeviceEnvelopeBase = z.object({ "v": z.literal(1), "kind": z.literal("evt"), "type": z.enum(["status", "data", "error", "log"]), "seq": z.number().int().gte(0), "t_boot_ms": z.number().int().gte(0), "payload": z.any() }).strict();
/**
 * `schemas/device/envelope.json`
 *
 * A discriminated union rather than the schema's literal `allOf`: json-schema-to-zod
 * renders an if/then table as `z.intersection(z.any(), z.any())`, which accepts any
 * payload on any `type` and reports success.
 */
export const DeviceEnvelopeSchema = z.discriminatedUnion('type', [
    DeviceEnvelopeBase.extend({ type: z.literal('status'), payload: DevicePayloadStatusSchema }),
    DeviceEnvelopeBase.extend({ type: z.literal('data'), payload: DevicePayloadDataSchema }),
    DeviceEnvelopeBase.extend({ type: z.literal('error'), payload: DevicePayloadErrorSchema }),
    DeviceEnvelopeBase.extend({ type: z.literal('log'), payload: DevicePayloadLogSchema }),
]);
/** Shared fields of `schemas/plan-channel/envelope.json`, before the payload is pinned down. */
const PlanChannelEnvelopeBase = z.object({ "v": z.literal(1), "kind": z.enum(["req", "res"]), "type": z.enum(["plan", "plan_ack"]), "id": z.string().regex(new RegExp("^[A-Za-z0-9._:-]{1,128}$")), "ts": z.string().datetime({ offset: true }), "payload": z.record(z.string(), z.any()) }).strict();
/**
 * `schemas/plan-channel/envelope.json`
 *
 * A discriminated union rather than the schema's literal `allOf`: json-schema-to-zod
 * renders an if/then table as `z.intersection(z.any(), z.any())`, which accepts any
 * payload on any `type` and reports success.
 */
export const PlanChannelEnvelopeSchema = z.discriminatedUnion('type', [
    PlanChannelEnvelopeBase.extend({ type: z.literal('plan'), kind: z.literal('req'), payload: PlanChannelPayloadPlanSchema }),
    PlanChannelEnvelopeBase.extend({ type: z.literal('plan_ack'), kind: z.literal('res'), payload: PlanChannelPayloadPlanAckSchema }),
]);
